#!/bin/bash

ADDORUNCOMMENTLINE_DESCRIPTION='If LINE or commented-out LINE do not exist in at least one (all with --all) of\nthe passed FILE(s), asks the user whether to update it so that comment prefix\n(/ suffix) gets removed (in the first file to match, and only the first such\nmatching comment there), or LINE appended (at the end of the last FILE).' \
ADDORUPDATELINE=containedOrAddOrUpdateLine \
exec addOrUncommentLine "$@"
